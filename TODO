- If you first select an engine under e.g. New Game, and then exit and add a
  new engine, it will maybe be selected under New Game because this is purely
  done by index, not by name or something.
- Make fields red if an error occured.
